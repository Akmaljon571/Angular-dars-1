export interface Items {
    id: number
    image: string,
    title: string,
    color: string,
    ml: number,
    count: number,
    price: number,
    oldPrice: number
}

export interface IParam {
    id: number,
    change: boolean
}